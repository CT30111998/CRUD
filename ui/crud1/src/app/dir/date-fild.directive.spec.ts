import { DateFildDirective } from './date-fild.directive';

describe('DateFildDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFildDirective();
    expect(directive).toBeTruthy();
  });
});

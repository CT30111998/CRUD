import { FloDirDirective } from './flo-dir.directive';

describe('FloDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FloDirDirective();
    expect(directive).toBeTruthy();
  });
});

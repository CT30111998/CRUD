import { MyDiraectiveDirective } from './my-diraective.directive';

describe('MyDiraectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDiraectiveDirective();
    expect(directive).toBeTruthy();
  });
});
